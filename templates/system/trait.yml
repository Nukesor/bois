# The default directory to where files will be deployed
#
#target_dir: /etc

# The default permissions for all files inside this trait.
# Can be overwritten on a directory or filebases deeper down the file hierarchy.
#defaults:
#  owner: root
#  group: root
#  file_mode: 0o644
#  directory_mode: 0o755

# Which packages should be installed when this trait is active on a host.
# Check the bois book for supported package managers.
#packages:
#  pacman:
#    - linux-firmware

# Which services should be enabled when this trait is active on a host.
# Services are only enabled, not started, unless `start: true` is set.
# Check the bois book for supported service managers.
#services:
#  systemd:
#    - systemd-timesyncd
#    - backup.timer
#    - name: docker
#      start: true
