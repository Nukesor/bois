# The enabled traits for this host.
traits:
  - base

# The default directory to where files will be deployed
#
#target_dir: /etc

# The default permissions for deployed files and directories inside this host directory.
# Can be overwritten on a directory or filebases deeper down the file hierarchy.
#permission_defaults:
#  owner: root
#  group: root
#  file_mode: 0o644
#  directory_mode: 0o755

# Which packages should always be installed on this specific host.
# Check the bois book for supported package managers.
#packages:
#  pacman:
#    - linux-firmware

# Which services should always be enabled on this specific host.
# Services are only enabled, not started, unless `start: true` is set.
# Check the bois book for supported service managers.
#services:
#  systemd:
#    - ntp
#    - backup.timer
#    - name: docker
#      start: true
